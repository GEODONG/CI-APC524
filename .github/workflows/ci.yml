on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
    - main

jobs:
  tests:
    name: <test>
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
